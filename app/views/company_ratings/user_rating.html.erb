<h1><%=@company.name%> Rating</h1>
<%= semantic_form_for(@company_rating) do |f| %>


  <div class="form-inputs">
    <%= hidden_field_tag 'company_rating[company_id]' ,@company_rating.company_id %>
    <%= f.input :satisfaction, as: :slider, min: 0, max: 10, step: 1, color: 'red'%>
    <%= f.input :work_environment,as: :slider, min: 0, max: 10, step: 1, color: 'red' %>
    <%= f.input :cd, as: :slider, min: 0, max: 10, step: 1, color: 'red'%>
    <%= f.input :flexibility,as: :slider, min: 0, max: 10, step: 1, color: 'red' %>
    <%= f.input :relation, as: :slider, min: 0, max: 10, step: 1, color: 'red'%>
    <%= f.input :leadership,as: :slider, min: 0, max: 10, step: 1, color: 'red' %>
    <%= f.input :pride,as: :slider, min: 0, max: 10, step: 1, color: 'red' %>
    <%= f.input :opportunities, as: :slider, min: 0, max: 10, step: 1, color: 'red'%>
    <%= f.input :opinion,as: :slider, min: 0, max: 10, step: 1, color: 'red' %>
    <%= f.input :stimulatingjob, as: :slider, min: 0, max: 10, step: 1, color: 'red'%>
    <%= f.input :innovation,as: :slider, min: 0, max: 10, step: 1, color: 'red' %>
    <%= f.input :management, as: :slider, min: 0, max: 10, step: 1, color: 'red'%>
    <%= f.input :compensation,as: :slider, min: 0, max: 10, step: 1, color: 'red' %>
    <%= f.input :workload, as: :slider, min: 0, max: 10, step: 1, color: 'red'%>
    <%= f.input :equality,as: :slider, min: 0, max: 10, step: 1, color: 'red' %>
    <%= f.input :objectives,as: :slider, min: 0, max: 10, step: 1, color: 'red' %>
    <%= f.input :cooperation,as: :slider, min: 0, max: 10, step: 1, color: 'red' %>
    <%= f.input :guidance, as: :slider, min: 0, max: 10, step: 1, color: 'red'%>
    <%= f.input :reward,as: :slider, min: 0, max: 10, step: 1, color: 'red' %>
    <%= f.input :career, as: :slider, min: 0, max: 10, step: 1, color: 'red'%>
    <%= f.input :transparancy,as: :slider, min: 0, max: 10, step: 1, color: 'red' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

	 